{
  "name": "ask-first",
  "private": true,
  "version": "0.1.0",
  "description": "A WebMCP demo where an agent proposes consequential actions and a human approves them one at a time, with a receipt.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --reporter=dot",
    "e2e": "node e2e/smoke.mjs",
    "lint:headers": "node scripts/check-headers.mjs",
    "check": "npm run typecheck && npm run test && npm run build && npm run lint:headers"
  },
  "devDependencies": {
    "happy-dom": "^15.11.7",
    "puppeteer": "^24.10.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
